<p id="notice"><%= notice %></p>

<h1>呟くん<%= image_tag 'tori.png', :size => '50x50'%></h1>

<div style="margin-bottom: 20px;"></div>

<table>
  <thead>
    <tr>
      <th>つぶやき</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @posts.each do |post| %>
      <tr>
        <td><%= post.title %></td>
        <td><%= link_to 'けす', post, method: :delete, data: { confirm: 'けすの？' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to 'つぶやく', new_post_path %>

